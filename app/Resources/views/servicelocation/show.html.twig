{% extends 'base.html.twig' %}

{% block body %}
    <h1>ServiceLocation</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ serviceLocation.id }}</td>
            </tr>
            <tr>
                <th>Longtitude</th>
                <td>{{ serviceLocation.longtitude }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ serviceLocation.latitude }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ serviceLocation.address }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ serviceLocation.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('servicelocation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('servicelocation_edit', { 'id': serviceLocation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
